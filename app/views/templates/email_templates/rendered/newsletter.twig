<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body style="line-height: 1.3; margin: 0 auto; max-width: 800px; padding: 15px 30px;">


<div class="head" style="background-color: #444; height: 44px; padding: 8px 4px;"><img src="{{ domain }}/img/full_logo.png"></div>
<div class="content" style="color: #444; font-family: 'Arial', sans-serif; font-size: 11pt; margin-left: auto; margin-right: auto; padding: 8px;">
    {% for section in sections %}
    <div class="newsletter-section" style="clear: both; min-width: 500px; padding: 30px 5px;">
        {{ section.content|raw }}
    </div>
    <div style="clear: both;"></div>
    {% endfor %}
</div>

<div class="footer" style="background-color: #aaa; clear: both; font-family: 'Arial', sans-serif; margin-top: 15px; padding: 15px; text-align: center;">
    <a href="{{ domain }}/unsubscribe" style="color: #555; font-size: 13px;">Unsubscribe</a> | <a href="{{ domain }}/newsletter/{{ newsletterId }}" style="color: #555; font-size: 13px;">View in Browser</a>
</div>
</body>
</html>