<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
<link rel="stylesheet" href="email_style.css">

<div class="head"><img src="{{ domain }}/img/full_logo.png"></div>
<div class="content">
    <p>User {{ user.getFirstName() }} {{ user.getLastName() }} requested a {{ requestType }} account
        {% if requestType != 'Admin' %} for {{ organizationName }}{% endif %}.</p>
    <p>Log into your admin account to approve or deny this request.</p>
</div>
</body>
</html>